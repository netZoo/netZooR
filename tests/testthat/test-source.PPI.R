context("test source PPI")

test_that("source.PPI works", {
  load("./testDataset.RData")
  # if(R.Version()$major=="3"){
  # actual_PPI <- source.PPI(tf,"10",83332)
  # expect_equal(actual_PPI,PPI.form.StringDBV10)}
}) 
